# 前端技术栈保持不动（Vue 2.6 + Vue CLI 3）

本仓库为 2020 年前后技术栈（Vue 2.6 / Vue CLI 3 / Element UI 2.13 / axios 0.18）。决定不做升级、保持原样。

原因：仓库定位是开源展示与产品推广素材，不是重开发；升级到 Vue 3 等于整体重写，与最小改动原则冲突。运行需要时使用老 Node 版本或加兼容参数（如 --openssl-legacy-provider），不升级代码。若未来要长期在此仓库上做功能开发，再单独立项评估升级。